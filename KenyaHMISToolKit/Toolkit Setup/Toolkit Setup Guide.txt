sudo apt -y remove kenyahmistoolkit
sudo rm -rf /opt/kehmisApplicationToolbox
sudo dpkg -i kenyahmistoolkit-2.0.deb
sudo apt -f install

#Check Java version running on your machine

echo $JAVA_HOME

sudo update-alternatives --config java
